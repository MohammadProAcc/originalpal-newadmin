import styled from 'styled-components'
import { Modal } from '@mantine/core'

export const MantineModal = styled(Modal)`
  .mantine-Modal-inner {
    display: flex;
    justify-content: center;
    align-items: center;
  }
`
